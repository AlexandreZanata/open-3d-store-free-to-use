# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.turbo
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# API dev uploads (writable fallback when /var/www is not available)
apps/api/storage/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Optional project-specific overrides (not part of harness)
.local/
.cursor/rules/_task-active.mdc

# Production module — real VPS secrets never in git (see production/README.md)
production/**
!production/README.md
!production/deploy-to-vps.sh
!production/env/
production/env/**
!production/env/README.md
!production/env/*.example
!production/ssh/
production/ssh/**
!production/ssh/README.md
!production/vps.env.example
production/vps.env
production/ssh/id_*
production/ssh/*.pem

# Kubeconfig / deploy artifacts (if added later)
kubeconfig
*.kubeconfig

# Environment and secrets
.env
.env.*
!.env.example
!.env.production.example

# OS and IDE
.DS_Store
.idea/
*.swp
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
test-results/
playwright-report/
e2e/.auth/

# Python tooling (harness scripts)
__pycache__/
.venv/
venv/
