# Nginx site config — deploy to /etc/nginx/sites-available/print3d.conf
# Full template in Phase 8. See docs/infrastructure/nginx.md

# TODO: HTTP → HTTPS redirect, SSL, static web root, /models/, /api/ proxy
