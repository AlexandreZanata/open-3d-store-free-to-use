# Local deploy target — copy to vps.env (never commit)

VPS_HOST=72.60.147.2
VPS_USER=root
VPS_PORT=22
VPS_APP_DIR=/var/www/print3d

# Temporary: use VPS IP until DNS is ready (other site uses the domain)
DOMAIN=72.60.147.2
VPS_USE_HTTPS=0

# If port 80 is taken by another site on the same VPS, use e.g. 8080
NGINX_HTTP_PORT=80
