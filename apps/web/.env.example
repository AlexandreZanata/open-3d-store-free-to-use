# REST API base URL (includes /api/v1)
VITE_API_BASE_URL=http://localhost:3001/api/v1

# Static assets base for model thumbnails and .glb files
# Use the storefront origin in dev — Vite proxies /models to the API (see vite.config.ts)
VITE_ASSETS_BASE_URL=http://localhost:5173
