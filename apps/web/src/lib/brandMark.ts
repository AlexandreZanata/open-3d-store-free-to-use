/** 2D corvo mark — same asset as header `BrandMark` and hero loading placeholder. */
export const BRAND_MARK_ICON_SRC = "/brand/corvo-logo.png";

/** Forces the PNG silhouette to solid `#141414` — hero fallback only (not header). */
export const BRAND_MARK_FALLBACK_CLASS = "brightness-0";

export const BRAND_MARK_SIZES = {
  sm: "h-9 w-[3.25rem]",
  md: "h-11 w-[4rem]",
  lg: "h-14 w-[5rem]",
} as const;
