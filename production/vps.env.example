# IP mode — copy to vps.env before first deploy (never commit vps.env)

VPS_HOST=YOUR_VPS_IP
VPS_USER=root
VPS_PORT=22
VPS_APP_DIR=/var/www/print3d

DOMAIN=YOUR_VPS_IP
VPS_USE_HTTPS=0

NGINX_HTTP_PORT=80
API_PORT=3101
