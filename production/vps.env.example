# Local deploy target — copy to vps.env (never commit)

VPS_HOST=72.60.147.2
VPS_USER=root
VPS_PORT=22
VPS_APP_DIR=/var/www/print3d

# Primary domain (no scheme) — used by nginx template substitution
DOMAIN=yourdomain.com.br
