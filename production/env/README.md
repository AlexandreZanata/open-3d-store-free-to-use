# Production env files

Copy `*.example` → real filenames (without `.example`). **Never commit** `api.env`, `web.env.production`, `admin.env`, or `docker.env`.

See [../../docs/infrastructure/vps-provisioning.md](../../docs/infrastructure/vps-provisioning.md).
